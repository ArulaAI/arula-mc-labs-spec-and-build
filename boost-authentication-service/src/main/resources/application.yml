server:
  port: 8081

spring:
  application:
    name: boost-authentication-service

management:
  endpoints:
    web:
      exposure:
        include: health,info

pgs:
  auth:
    # Deny by default: only these client ids may retrieve payer authentication results.
    authorized-clients: boost-order-processing
