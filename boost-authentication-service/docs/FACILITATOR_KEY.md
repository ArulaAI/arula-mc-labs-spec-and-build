# Facilitator key — not here

The Lab 2 facilitator key is **answer material**. It lives with the rest of the answer material,
under the workspace's protected reference directory:

```
.claude/reference/FACILITATOR_KEY.md
```

`reference_guard.py` denies a learner session access to it, and to this file, unless
`WORKBENCH_FACILITATOR=1` is set. Facilitators: see `.claude/reference/README.md`.
